const styles = {
  global: {
    html: {
      fontSize: "87.5%",
    },
    body: {
      bgColor: "bg",
    },
    "*, *::before, &::after": {
      borderColor: "blackAlpha.200",
    },
  },
};

export default styles;
